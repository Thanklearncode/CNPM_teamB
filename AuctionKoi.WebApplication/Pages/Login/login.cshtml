@page
@model AuctionKoi.WebApplication.Pages.Login.LoginModel
@{
    Layout = "~/Pages/Shared/login-layout.cshtml";
}


<!doctype html>
<div class="section">
    <div class="container">
        <div class="row full-height justify-content-center">
            <div class="col-12 text-center align-self-center py-5">
                <div class="section pb-5 pt-5 pt-sm-2 text-center">
                    <div class="card-3d-wrap mx-auto">
                        <div class="card-3d-wrapper" id="card-wrapper">
                            <div class="card-front">
                                <div class="center-wrap">
                                    <div class="section text-center">
                                        <h4 class="mb-4 pb-3">Đăng nhập</h4>
                                        <form method="post">
                                            <div class="form-group">
                                                <input type="email" name="Email" class="form-style" placeholder="Email" required>
                                                <i class="input-icon uil uil-at"></i>
                                            </div>
                                            <div class="form-group mt-2">
                                                <input type="password" name="Password" class="form-style" placeholder="Mật khẩu" required>
                                                <i class="input-icon uil uil-lock-alt"></i>
                                            </div>
                                            <button type="submit" class="btn mt-4">Đăng nhập</button>
                                            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                                            {
                                                <p class="text-danger mt-3">@Model.ErrorMessage</p>
                                            }
                                        </form>
                                        <p class="mb-0 mt-4 text-center">
                                            <a asp-page="../ForgotPassword/ForgotPassword" class="link">Quên mật khẩu</a>
                                        </p>
                                        <p class="mb-0 mt-4 text-center">
                                            Chưa có tài khoản? <a href="../Register/Register" class="link" id="toRegister">Đăng kí</a>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

